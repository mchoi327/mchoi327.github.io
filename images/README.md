Image naming convention:

- `news_N_slug.ext`: news images, ordered newest to oldest as displayed.
- `home_N_slug.ext`: homepage/profile assets.
- `header_N_slug.ext`: shared header or institutional logo assets.

Keep filenames lowercase except for established acronyms when there is a clear reason.
